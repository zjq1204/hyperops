# Development Dockerfile for HyperOps Frontend
FROM node:22-alpine

# Set working directory
WORKDIR /app

# Copy package files
COPY package*.json ./

# Install dependencies (in case node_modules volume is not available)
RUN npm install

# Note: node_modules, src, and config files will be mounted from host
# Make sure to run 'npm install' on the host first

# Expose Vite dev server port
EXPOSE 3000

# Start development server
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
